// import { useSelector } from "react-redux";
// import { useParams } from "react-router";

const Categories=()=>{
    // const {categ} = useParams();
    // const recepies = useSelector((state)=> state.filter((r)=> r.category === categ))
    // return(<div>
    //     {recepies.map((recepie)=>(
    //         <div key={recepie.id}>

    //         </div>
    //     ))}
    // </div>)
}

export default Categories;